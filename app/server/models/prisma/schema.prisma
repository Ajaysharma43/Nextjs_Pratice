generator client {
  provider = "prisma-client-js"
}

datasource Database {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model todo {
  id  Int @id @default(autoincrement())
  name String
  todo String
}
